<IfModule mod_rewrite.c>
  RewriteEngine On

  # Redirect non-www to www (megauravedics.com -> www.megauravedics.com)
  RewriteCond %{HTTP_HOST} ^megauravedics\.com$ [NC]
  RewriteRule ^(.*)$ https://www.megauravedics.com/$1 [R=301,L]
</IfModule>
